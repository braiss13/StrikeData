@page
@model StrikeData.Pages.TeamData.WinTrendsModel
@{
    ViewData["Title"] = "Win Trends";
}

<h1 class="page-title">Win Trends</h1>
<p class="lead">This page displays the win trends of teams in the league.</p>

@* 
<table class="table">
    <thead>
        <tr>
            <th>Team</th>
            <th>Record</th>
            <th>Win %</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var team in Model.Teams)
        {
            <tr>
                <td>@team.Name</td>
                <td>@team.OverallRecord</td>
                <td>@team.WinPercentage?.ToString("P2")</td>
            </tr>
        }
    </tbody>
</table> *@
